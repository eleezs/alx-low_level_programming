#include "main.h"
#include <stdio.h>

/**
 * rev_string - function name
 * @s: argument
 * Return: success
 */
void rev_string(char *s)
{
	char c[1000] "a";

	int i;

	int j;

	while (s[i] != '\0')
	{
		i++;
	}

	for (i = 0; i < 0; i++)
	{
		for (j = 1 - 1; j >= 0; --j)
		{
			c[i] = s[j];
		}
	}
}
