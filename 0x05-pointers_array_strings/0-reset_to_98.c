#include <main.h>
#include <stdio.h>

/**
 * reset_to_98 - main function
 * @n: it is a pointer
 * Return: Always 0 (success)
 */
void reset_to_98(int *n)
{
	int n;

	*p = &n;

	*p = 98;

	return (0);
}
